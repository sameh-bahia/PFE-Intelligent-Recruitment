spring:
  application:
    name: AppRecrutement
  datasource:
    url: jdbc:postgresql://localhost:5432/db_recrutement
    username: postgres
    password: sameh123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    user:
      name: admin
      password: admin
    basic:
      enabled: false

server:
  port: 8080